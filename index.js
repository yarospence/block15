
//create an app that allows the user to select his yogurt preferences
//the user needs to be able to select different flavors

//create an array of chocolate, strawberry, vanilla, and melon.
const flavors = ([`chocolate`, `strawberry`, `vanilla`, `melon`]) => {
//the response needs to be able to seperate the different flavors
  const userFlavor = flavors.split(` `);
  return userFlavor([]);
}

//the user needs to be able to decide how many desired
userInput = prompt(`Order your Flavor!`);

